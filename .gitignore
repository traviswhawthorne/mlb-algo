# Python
__pycache__/
*.py[cod]
*.pyo

# IDE / tooling
.claude/
.next/
*.local.json

# GCP service account credentials — never commit
*service_account*.json
*-494820-*.json
credentials*.json
gcp_key*.json

# Cache directory (large, re-generated on each run)
cache/

# Excel outputs (written to Drive, not stored in repo)
MLB_Picks.xlsx
MLB_Results_*.xlsx
MLB_Tracker.xlsx
Bullpen_Validation*.xlsx
MLB_Backtest*.xlsx
Starter_IP_Report.xlsx

# Odds CSV
oddsData.csv

# Debug / scratch files
debug_*.html
debug_output.txt
*.log
logs/

# Backtest log
backtest_odds_run.log

# Analysis / debug scripts (not needed in cloud)
analyze_*.py
backtest*.py
check_*.py
park_factors_analysis.py
starter_ip_by_era.py
tbd_pitcher_era.py
validate_*.py
make_workflow_doc.py
auto_run.py
test_sbr_debug.py

# Batch scripts (Windows-only)
*.bat

# Word doc
WORKFLOW.docx

# Data cache — include 2025/2026 stats so GitHub Actions runners reuse them.
# Exclude the large backtest-only boxscore cache and old year data.
data/boxscore_cache/
data/*_2018_*
data/*_2019_*
data/*_2021_*
data/*_2024_*
# Old per-day picks files are regenerated by coordinator; only keep recent ones
# (coordinator commits today's picks back to the repo automatically)
